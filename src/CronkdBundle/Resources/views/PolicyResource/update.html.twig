{% extends '::base.html.twig' %}

{% block content %}
    <div class="row-fluid">
        <div class="col-md-12">

            <ol class="breadcrumb">
                <li><a href="{{ path('world_configure', {'world': world.id}) }}">Configure {{ world.name }}</a></li>
                <li><a href="{{ path('policy_update', {'id': policy.id}) }}">Update {{ policy.name }}</a></li>
                <li class="active">Update {{ policyResource.resource.name }} Policy for {{ policy.name }}</li>
            </ol>

            <h1>Update {{ policyResource.resource.name }} Policy for {{ policy.name }}</h1>
            {% include 'CronkdBundle:KingdomPolicyResource/partials:form.html.twig' with {'action': 'Update'} %}

        </div>
    </div>
{% endblock %}